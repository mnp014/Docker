 - The following runs `bash` inside the container in interactive mode:
 
   ``` docker exec -it <container name> /bin/bash ```
---
 - The same can be used to run `any command` inside the container:
 
   ``` docker exec -it <container name> <command> ```
---   
